mixin logo(src, alt)
	.logo-wrap
		if title!=='Главная'
			a(class="logo-link" href="index.html")
				img(class="logo-img" src=`${src}`, alt=`${alt}`)
		else 
			a
				img(class="logo-img" src=`${src}`, alt=`${alt}`)